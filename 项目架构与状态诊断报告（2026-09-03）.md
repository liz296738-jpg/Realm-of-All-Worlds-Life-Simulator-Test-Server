# 万界人生模拟器 —— 项目架构与状态诊断报告

> **生成日期**：2026-09-03
> **目标读者**：AI 架构师（深度分析与重构规划用）
> **分析方法**：全量代码扫描 + 依赖图分析 + 数据流追踪 + 15 子代理并行模块剖析
> **状态**：客观诊断，不含修改建议
> **基线**：本报告为 2026-08-12 旧版报告的重制版，逐节标注了相对旧版的架构演进。

---

## 目录

1. [技术栈与依赖环境](#1-技术栈与依赖环境)
2. [核心目录与文件树](#2-核心目录与文件树)
3. [架构与数据流拆解](#3-架构与数据流拆解)
   - [3.1 状态管理](#31-状态管理)
   - [3.2 本地存储与数据持久化](#32-本地存储与数据持久化)
   - [3.3 API 交互层](#33-api-交互层)
   - [3.4 推演机制](#34-推演机制)
4. [代码坏味道与架构隐患](#4-代码坏味道与架构隐患)
5. [相对旧版报告的关键演进总结](#5-相对旧版报告的关键演进总结)

---

## 1. 技术栈与依赖环境

### 前端

| 类别 | 技术 | 版本 | 备注 |
|------|------|------|------|
| **框架** | Vue 3 (Composition API / SFC) | ^3.5.0 | 纯 `<script setup>`，无 TypeScript（**无变化**） |
| **构建工具** | Vite | ^5.4.0 | 开发代理 `/api` → `127.0.0.1:8000`；生产由后端托管 `dist/` |
| **CSS** | Tailwind CSS | ^3.4.0 | 全局样式 + 原子类混用 |
| **Markdown** | markdown-it | ^14.1.0 | 回合叙述 HTML 渲染 |
| **运行时** | Bun | — | `bun.lock` 锁文件，Node 亦可运行 |

**依赖极简**：前端运行时依赖仍仅 `vue` + `markdown-it` 两项，无状态管理库（Pinia）、无路由库（vue-router）、无 UI 组件库。

### 后端

| 类别 | 技术 | 备注 |
|------|------|------|
| **HTTP 框架** | FastAPI | 异步框架，核心逻辑全程同步（`def`） |
| **服务器** | uvicorn | `uvicorn[standard]` |
| **AI SDK** | openai >=1.0 | 实际调用 DeepSeek API（`base_url` 指向 `api.deepseek.com`） |
| **数据校验** | pydantic | 请求体模型 |
| **环境变量** | python-dotenv | 仅 `.env.example`，无 `.env`（BYOK 合法） |
| **文档解析** | python-docx | 上传小说建世界时的 .docx 提取 |
| **表单上传** | python-multipart | **🆕 新增**：小说上传的 `multipart/form-data` 支持 |
| **测试** | pytest + httpx | 23 个测试文件（旧版 17 个） |

### AI 模型

- **模型**：`deepseek-chat`（全局常量 `MODEL`）
- **温度**：叙述 0.85（流式）/ 0.3（结算 JSON）
- **输出格式**：`response_format: json_object`（统一回合调用）

---

## 2. 核心目录与文件树

> 与旧版最大的结构变化：**后端从「单文件 God Object」演进为「按职责垂直切分的多包结构」**。

```
万界人生模拟器/
├── frontend/                         # Vue 3 前端
│   ├── src/
│   │   ├── App.vue                   # ⭐ 根组件：视图路由 + 开局/读档/续玩流程编排
│   │   ├── store.js                  # ⭐ 全局状态仓库（readonly 视图 + Actions 模式 + 主题样式）
│   │   ├── api.js                    # ⭐ API 客户端：SSE 流消费 + REST 封装 + 超时/中断控制
│   │   ├── md.js                     # Markdown 渲染 + 选项块剥离（前端副本）
│   │   ├── main.js                   # 入口
│   │   ├── views/
│   │   │   ├── HomeView.vue          # 首页：世界广场 / 建世界
│   │   │   └── GameView.vue          # 游戏主视图：SSE 消费 + 回合提交 + undo
│   │   └── components/
│   │       ├── CreationWizard.vue    # 魂兽大陆专属 5 步创建向导
│   │       ├── GenericWizard.vue     # 自建世界通用创建向导
│   │       ├── CharacterCard.vue     # 角色卡预览
│   │       ├── NarrativeStream.vue   # 叙述流展示
│   │       ├── OptionsBar.vue        # 选项栏 + 自由输入 + 后悔
│   │       ├── StatusPanel.vue       # 状态抽屉（level_field 动态驱动）
│   │       ├── CharacterPanel.vue    # NPC 情报抽屉 + AI 提取 + 备注自动保存
│   │       ├── SavePanel.vue         # 存档/读档/导出面板
│   │       ├── SettingsPanel.vue     # 设置面板：主题/文字数量/数据管理/订阅（可折叠）
│   │       └── ActivationPanel.vue   # 订阅/激活码弹窗
│   └── index.html
│
├── backend/                          # Python 后端（多包结构）
│   ├── main.py                       # 🟢 纯入口（57 行）：FastAPI 初始化 + CORS + 静态托管 + 旧架构残留工具函数
│   ├── api/
│   │   ├── __init__.py
│   │   └── routes.py                 # ⭐ 路由层（855 行）：19 个端点 + 回合引擎 _run_turn
│   ├── auth/
│   │   ├── __init__.py
│   │   └── subscription.py           # ⭐ 订阅门禁（138 行）：激活码校验 + 免费试玩 + 并发锁
│   ├── llm/
│   │   ├── __init__.py
│   │   └── deepseek_client.py        # ⭐ LLM 通信（163 行）：BYOK 双模式 + 流式/结算/统合三接口
│   ├── game/
│   │   ├── __init__.py
│   │   ├── session_manager.py        # ⭐ 会话缓存（77 行）：2 小时 TTL 驱逐 + 冷热加载
│   │   ├── game_engine.py            # ⭐ 状态引擎（102 行）：delta 落账 + 好感衰减 + 季节推算
│   │   ├── state_schema.py           # ⭐ 权威状态模型（273 行）：default_state / validate_delta
│   │   ├── prompt_builder.py         # ⭐ 提示词组装（405 行）：零硬编码状态摘要 + 反水字数铁律
│   │   ├── worlds.py                 # 世界规格（167 行）：加载/校验/缓存/原子写
│   │   ├── world_builder.py          # 建世界流水线（209 行）：小说抽样 → DeepSeek → 规格校验
│   │   ├── save_manager.py           # 持久化（162 行）：state/turns/undo/存档点/激活登记
│   │   ├── novel_parser.py           # 小说解析：TXT 编码探测 + DOCX 提取 + 抽样
│   │   ├── exporter.py               # 剧情导出：回合叙述 → Markdown
│   │   └── activation_codes.py       # 激活码池：366 个随机码，每年循环
│   ├── worlds/
│   │   ├── douluo.json               # 魂兽大陆
│   │   ├── gebi.json                 # 隔壁的租客
│   │   ├── taishangfuli.json         # 太上浮黎
│   │   ├── wanwusheng.json           # 万物生
│   │   ├── shanhe.json               # 🆕 山河万象
│   │   ├── zhujie.json               # 🆕 诸界魔典
│   │   ├── zhenshi.json              # 🆕 真实人生
│   │   ├── shuguang.json             # 🆕 曙光
│   │   ├── shengluolan.json          # 🆕 圣罗兰
│   │   └── yongzhou.json             # 🆕 永昼
│   ├── prompts/                      # 规则书（system_prompt.txt 等）
│   ├── data/                         # 运行时数据（激活码池 / 自定义世界 / 存档）
│   ├── generate_code.py              # 激活码池生成/查看工具
│   └── tests/                        # 23 个 pytest 文件
│
├── docs/
│   ├── 创作者建世界模板.md
│   └── 新世界创建与GitHub发布指南.md
├── render.yaml                       # Render 平台部署
└── Dockerfile
```

### 核心文件作用速览（含行数对比）

| 文件 | 行数 | 一句话职责 | 旧版行数 |
|------|------|-----------|---------|
| `backend/main.py` | **57** | 纯入口：FastAPI 初始化 + CORS + 静态托管 + 旧架构残留工具 | ~~1138~~ |
| `backend/api/routes.py` | **855** | 路由层 + 回合引擎 `_run_turn`：19 个端点 + 建世界限流 + 越权防线 | — |
| `backend/auth/subscription.py` | **138** | 订阅门禁：激活码校验（UTC+8 墙钟）+ 免费试玩计数 + 线程锁 | — |
| `backend/llm/deepseek_client.py` | **163** | LLM 通信：BYOK 双模式 + 三接口 + 中文友好错误映射 | — |
| `backend/game/session_manager.py` | **77** | 会话内存缓存：2 小时 TTL 驱逐 + 冷热加载 | — |
| `backend/game/game_engine.py` | 102 | 状态应用引擎：delta 落账 + 好感衰减 + 季节推算 | 102（不变） |
| `backend/game/state_schema.py` | 273 | 权威状态模型：default_state / validate_delta | 272（不变） |
| `backend/game/prompt_builder.py` | 405 | 提示词工厂：零硬编码状态摘要 + 反水字数铁律 + 双上限裁剪 | 424 |
| `frontend/src/store.js` | 372 | 状态仓库：readonly 视图 + Actions 模式 + 主题样式注入 | 177 |
| `frontend/src/components/SettingsPanel.vue` | 304 | 设置面板：主题/文字数量/数据管理/订阅四分组（可折叠） | — |

---

## 3. 架构与数据流拆解

### 3.1 状态管理

#### 前端状态（`store.js` — readonly 视图 + Actions 封装）【🟢 已重构】

旧版报告的核心批评是「组件直接对 reactive 对象赋值，无从追踪」。**当前已彻底改造成单向数据流**：

```js
// 私有可变状态（仅模块内部可见）
const _game = reactive({ sessionId, state, narrative, streaming, options, ... })

// 只读视图（组件只允许读，不允许直接写）
export const game = readonly(_game)

// 唯一修改入口：命名 Actions
export function setGameState(state) { _game.state = state; ... }
export function commitTurn({ narrative, options, ... }) { ... }
export function setStreaming(v) { ... }
```

**五个状态域**与旧版一致（`game` / `ui` / `worlds` / `entitlement` / `draft`），但：
- 组件不再能 `game.narrative = ''` —— 只能调用 `clearNarrative()`。
- UI 面板通过 `togglePanel(name, isOpen)` 白名单校验后写入，杜绝任意属性注入。
- 视图切换通过 `setUiView(view)` 的 `VALID_VIEWS` 集合校验。
- 旧版「`setGameState` 与 GameView 内 `setState` 同义重复」问题已消除——GameView 直接复用 store 的 `setGameState`。

**主题样式系统（🆕 新增）**：`_ui` 域新增 `themeColor` / `baseFontSize` / `bgImage` 三个字段，经 `updateThemeSettings()` / `updateBackgroundImage()` 两个 Action 写入并即时注入 DOM 根节点的 CSS 变量：

```css
:root {
  --theme-color: #d97706;       /* 主题色（tailwind primary 别名） */
  --base-font-size: 16px;       /* 基础字号 12-24px */
  --theme-bg: color-mix(...);   /* 背景色 = 主题色 15% 混入近黑 #0c0a09 */
  --theme-bg-image: ...;        /* 自定义背景图（含暗色遮罩） */
}
```

- 主题色经 tailwind `colors.primary = 'var(--theme-color)'` 打通全站 `bg-primary` / `text-primary`，替换原硬编码 `amber-600`。
- 背景色由 `color-mix(in srgb, 主题色 15%, #0c0a09)` 派生，换主题色即同步换背景；自定义背景图上传经前端 canvas 压缩（限宽 1920px、JPEG 0.8）后存 base64。
- 主题 / 文字数量 / 数据管理 / 订阅四个功能统一收敛进 `SettingsPanel.vue`（四个可折叠分组，默认收起），`FreedomControl.vue` 已删除，首页底部的数据备份与订阅入口一并移除。

**残留问题**（详见第 4 章）：
- `draft` 对象仍混合「临时草稿数据」与「持久化配置（apiKey/freedom）」，语义未彻底分离。
- `patchNpcState(name, update)` 直接 `Object.assign` 到 `game.state.npcs`，是 Actions 模式下唯一的「局部就地修改」旁路。

#### 后端状态（`session_manager.py` — TTL 驱逐）【🟢 已重构】

旧版 `_SESSIONS` 是 `main.py` 里的模块级裸 dict、**无 TTL**。当前抽离为独立模块并引入生命周期管理：

```python
SESSION_TTL = 7200   # 2 小时
_SESSIONS: dict[str, dict] = {}   # {session_id: {"data": session_dict, "last_active": float}}

def _cleanup_stale(): ...          # 驱逐超时会话（O(n)）
def _new_session(...): ...         # 构造 + 写入缓存 + 触发清理
def _load_session(sid): ...        # 缓存命中刷新活跃时间；未命中冷启动落盘
def remove_session(sid): ...       # 删除存档时移除
```

- **TTL 已实现**：会话超 2 小时未活跃即驱逐，内存泄漏隐患消除。
- 会话对象仍是**裸 dict**（无 dataclass / TypedDict），静态类型收益未兑现。
- 冷启动恢复保留（`load_state` + `load_turns`）。

### 3.2 本地存储与数据持久化

#### 前端 localStorage（7 个 key）

| Key | 用途 |
|-----|------|
| `douluo_client_id` | 浏览器身份标识 |
| `douluo_api_key` | 玩家 DeepSeek API Key（BYOK） |
| `douluo_freedom` | 自由度档位 1-5 |
| `douluo_sub_code` | 激活码 |
| `wanjie_theme_color` | 🆕 主题色（默认 `#d97706`） |
| `wanjie_font_size` | 🆕 基础字号（12-24px） |
| `wanjie_bg_image` | 🆕 自定义背景图（base64 data URL） |

**安全隐患不变**：API Key 与激活码仍明文存 localStorage（XSS/恶意扩展可读）；激活码可跨设备转发。

#### 后端文件持久化（与旧版一致，新增会话缓存层）

```
backend/data/
├── saves/{session_id}/
│   ├── state.json / history.jsonl / turns.json / undo.json
│   └── savepoints/{sid}-{ts}.json
├── activations.json            # 订阅登记表（原子写：tmp → os.replace）
└── worlds/{world_id}.json      # 自定义世界规格（原子写）
```

**变更点**：
- `save_activations` 与 `save_custom_world` 均采用 `tmp → os.replace` 原子写。
- `save_state` 仍是「两文件分步写」不原子（state.json 与 history.jsonl 分两次调用）——极端崩溃下可能不同步。旧版已识别，未改。
- 存档归属校验新增 `check_owner` + 路径穿越白名单 `_assert_safe_id`。

### 3.3 API 交互层

#### 端点全景（19 个，旧版 18 个）

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/health` | GET | 健康检查 |
| `/api/saves` | GET | 存档列表（按 client_id 隔离） |
| `/api/new-game` | POST | 开新档，SSE 流式开场 |
| `/api/act` | POST | 提交行动，SSE 流式新回合 |
| `/api/activate` | POST | 激活码核验 |
| `/api/entitlement` | POST | 查询订阅/试玩状态 |
| `/api/resume` | POST | 继续游戏（不调 AI） |
| `/api/undo` | POST | 后悔（退回上一回合） |
| `/api/save` | POST | 存档点 |
| `/api/load` | POST | 加载存档点 |
| `/api/delete` | POST | 删除会话 |
| `/api/update-npcs` | POST | NPC 备注更新 |
| `/api/extract-npc-profiles` | POST | AI 提取 NPC 情报 |
| `/api/export-all` | POST | 导出世界 + 全部存档 |
| `/api/import-all` | POST | 导入备份（指纹覆盖归属） |
| `/api/export` | POST | 导出剧情 Markdown |
| `/api/worlds` | GET | 世界广场列表 |
| `/api/worlds/build` | POST | 上传小说建世界（限流 5 次/小时） |
| `/api/worlds/delete` | POST | 删除自定义世界 |

**越权防线（新增）**：`_resolve_world` 对自定义世界校验作者归属，非作者返回 404 且不泄露世界存在；`check_owner` 保护所有存档操作。

#### SSE 流协议（不变）

```
event: text    → data: {"content": "叙述片段"}
event: delta   → data: {"state", "options", "notes", "event", "can_undo"}
event: done    → data: {}
event: error   → data: {"message": "友好中文提示"}
```

#### API Key 路由（BYOK 双模式，不变）

```
玩家请求携带 api_key?
  ├─ 是 → 用玩家 Key（DeepSeek 计费，站点零成本）
  └─ 否 → 服务端 _client（DEEPSEEK_API_KEY 环境变量）
            └─ 无 → RuntimeError("NO_SERVER_KEY") → 引导填 Key
```

### 3.4 推演机制

#### 回合生命周期（单次结构化 JSON 调用，架构不变）

```
[玩家选择 action]
    ▼
GameView.submit(action)
    ├─ commitTurn()          ← 归档上一回合（DOM 原位保留，不跳页）
    ├─ POST /api/act
    ▼
后端 _run_turn()（api/routes.py）
    ├─ 选项字母扩展（"C" → "选择了选项 C：..."，防 AI 上下文失忆）
    ├─ build_unified_messages()
    │   ├─ system_prompt(world)   ← 规则书 + 反水字数铁律
    │   ├─ state_summary(state)   ← 零硬编码，world spec 驱动
    │   └─ _history_messages()    ← 双上限裁剪（条数 + token）
    ├─ _call_turn(msgs, api_key, max_tokens=tier+6000)  ← 单次 JSON 调用
    │   └─ 重试一次（纠偏 nudge）→ 失败兜底 {}
    ├─ 模拟流式推送（按中文标点边界切分逐块 yield）
    ├─ _normalize_options()      ← 选项补全（缺 label → ABCD；缺推荐 → 首个推荐）
    ├─ undo 快照保存（deepcopy，最多 20 层）
    ├─ apply_delta(state, delta, world)  ← validate_delta 钳制 → 落账
    ├─ save_turns() + save_state()       ← 每回合持久化
    ├─ _bump_trial()                     ← 免费试玩计数（尽力而为）
    ├─ 每 10 回合自动存档 create_savepoint
    └─ yield delta SSE → yield done
```

#### 关键设计变更

- **反水字数铁律（🆕）**：`prompt_builder.py` 全局 `_ANTI_LOOP_RULES` 追加到所有 system prompt 末尾，禁止复读/水字数/静态占位，强制惩罚敷衍输入、推动剧情增量。
- **token 预算调整（🆕）**：统一调用 `max_tokens = tier["max_tokens"] + 6000`（旧版 +1200）。为 options/delta/notes/event 预留充足空间，消除「复杂场景截断成非法 JSON → 只剩单选项」的失败路径。
- **选项字母扩展（🆕）**：将玩家选择的裸字母扩展为完整选项文本，缓解长对话下 AI 的上下文失忆。
- **世界特定逻辑硬编码已消除（🟢）**：旧版 4 套 `_douluo_summary()` 等硬编码函数已删除，`state_summary()` 零硬编码、完全由 `world["state_template"]` 与 `creation_schema` 驱动；前端 `StatusPanel` 从 `isDouluo` 分支改为 `meta.level_field` 动态驱动。

#### 耦合评估（更新）

| 维度 | 状态 | 说明 |
|------|------|------|
| 提示词构造 ↔ UI | **低耦合** | `prompt_builder.py` 不感知前端 |
| 状态应用 ↔ UI | **低耦合** | `game_engine.py` 纯函数式 |
| 世界规格 ↔ 引擎 | **低耦合（改善）** | 零硬编码，世界差异全由 spec 驱动 |
| 回合流 ↔ HTTP | **高耦合** | `_run_turn()` 仍是 SSE 生成器，与 `StreamingResponse` 紧绑定 |
| 会话管理 ↔ 业务逻辑 | **中耦合（改善）** | 已抽离 `session_manager.py`，但路由内仍直接读写 `sess[...]` 裸 dict |

---

## 4. 代码坏味道与架构隐患

### ✅ 已解决的旧版隐患

| 旧版隐患 | 严重度 | 当前状态 |
|---------|--------|---------|
| `main.py` God Object（1138 行） | 🔴 | **已拆解**——拆为 10 个职责单一模块，`main.py` 仅剩 57 行入口 |
| `_SESSIONS` 无 TTL → 内存泄漏 | 🔴 | **已实现**——`session_manager.py` 2 小时 TTL 驱逐 |
| 前端全局可变状态无约束 | 🟡 | **已改善**——`readonly` 视图 + Actions 单向数据流 |
| 世界特定逻辑硬编码 | 🟡 | **已消除**——零硬编码 `state_summary` + `level_field` 动态驱动 |
| 同义函数重复（setGameState/setState） | 🟡 | **已消除**——统一复用 store 的 `setGameState` |
| 无结构化日志 | 🟢 | **已改善**——`logging` 模块 + `logger.warning/error` 已接入核心路径 |

### 🔴 P0 — 测试套件失修（1 个测试失败）【🆕 新发现，可观测 Bug】

**文件**：[`tests/test_freedom.py`](backend/tests/test_freedom.py) — `test_freedom_max_tokens_passed_through`

**实测结果**（`pytest -q`）：

```
1 failed, 213 passed, 1 warning
FAILED tests/test_freedom.py::test_freedom_max_tokens_passed_through - AssertionError: assert 9200 == 4400
```

**根因**：近期提交 `efd6f8f` 将统一调用 token 预算从 `+1200` 调整为 `+6000`，但该测试仍断言旧的 `4400`（`3200 + 1200`）。实际值 `9200`（`3200 + 6000`）证明**代码已改、测试未同步**。

**后果**：CI/本地测试红字，掩盖真实回归；`README.md` 中「143 passed」的声明也已过时（实际 214 个用例）。

### 🟡 P2 — `routes.py` 仍是 855 行的「次 God Object」

God Object 虽已从 `main.py` 拆出，但 **`_run_turn` 这个核心业务流水线（约 140 行）与 19 个路由定义、请求模型、建世界限流、越权防线同挤在 `routes.py`**。这是「控制器层」合理规模的临界点：

- `_run_turn` 是业务逻辑（服务层），却与 HTTP 路由定义混在同一文件，脱离 HTTP 上下文无法独立单测（旧版遗留问题未彻底解决）。
- 建议可拆 `game_service.py`（回合引擎）与 `world_routes.py`（世界 CRUD），但当前规模尚可接受。

### 🟡 P2 — 旧架构残留死代码

**文件**：[`main.py:24`](backend/main.py#L24) `_strip_options_block()`

函数自注释明确标记「新架构（_call_turn 统一 JSON 调用）中已不再使用，保留以备回退」，但仍挂在 `main.py` 入口文件中。前端 [`md.js`](frontend/src/md.js) 的 `stripOptionsBlock` 仍在用于渲染，两者是「前后端镜像实现」的旧版残留——一端已死、一端仍活。

### 🟡 P2 — 静默吞异常模式仍在

全量扫描确认旧版识别的「静默吞异常」模式未消除，仅从 `pass` 升级为 `logger.warning`：

```python
# 试玩计次：登记表写失败不拖垮本回合
except Exception as e:
    logger.warning(f"试玩计次失败 (session={session_id}): {e}")
# 自动存档：每 10 回合
except Exception as e:
    logger.warning(f"自动存档失败 (session={session_id}): {e}")
```

- **自动存档失败无反馈**：玩家第 90 回合后若存档点写失败，重启只能回退更早存档。
- **试玩计数失败**：可能被刷免费试玩（计数器写失败不报错）。
- **前端 `loadWorlds` / `refreshEntitlement`**：`catch { /* 后端未启动等场景静默 */ }` 仍无用户可见反馈。

### 🟡 P2 — `state_schema.py` 仍「混合专属 + 通用」字段

`validate_delta` 与 `default_state` 内部仍混合处理「魂兽大陆专属字段（soul_level/soul_rings）」与「通用多世界字段」。旧版报告的「命名不一致」表（`soul_level` vs `level_field`）中，`level_field` 抽象已在前端落地，但后端 schema 层对魂兽大陆仍存在向后兼容分支。

### 🟡 P3 — 多时区混用

- 订阅门禁用**中国时区 UTC+8**（`_cn_now()`）。
- 导出时间戳用 **UTC**（`datetime.now(timezone.utc)`）。
- 前端展示用**浏览器本地时区**。

三种时区仍混用（旧版已识别，未改）。跨年码池日期边界逻辑（`subscription.py` 内 `_verify_code`）为最高风险区。

### 🟢 P4 — 缺少输入校验层（不变）

- 玩家自由输入 `action` 直接入 prompt，越狱指令防御仍依赖 DeepSeek 自身内容安全。
- `extract_npc_profiles` 的 `npc_names` 列表直接拼入 prompt，未做转义/长度限制。

### 🟢 P4 — 测试覆盖（改善但仍有缺口）

23 个测试文件（旧版 17 个），覆盖 core 逻辑 + 新增的 BYOK/entitlement/worlds/build/path_safety。但：
- 无 `/api/act` SSE 流的端到端集成测试。
- 无前端 Vue 组件测试。
- `_run_turn()` 作为最复杂核心流水线，仍无直接单测（被生成器与 HTTP 紧绑定阻碍）。

### 📊 架构隐患总结矩阵（更新）

| 隐患 | 严重度 | 影响范围 | 当前是否引发可观测 Bug |
|------|--------|---------|----------------------|
| 测试套件失修（token 预算断言过期） | 🔴 高 | CI/回归验证 | **是**（`test_freedom` 红字失败） |
| `routes.py` 855 行次 God Object | 🟡 中 | 后端变更/可测试性 | 否（技术债累积） |
| 旧架构残留死代码（`_strip_options_block`） | 🟡 中 | 代码维护 | 否 |
| 静默吞异常 | 🟡 中 | 存档/订阅计数 | 是（自动存档失败无提示） |
| state_schema 专属+通用混合 | 🟡 中 | 新增世界 | 否 |
| 多时区混用 | 🟡 中 | 订阅激活 | 潜在（边界条件） |
| SSE 生成器与 HTTP 紧耦合 | 🟡 中 | 可测试性 | 否 |
| 前端 draft 混草稿/配置 | 🟢 低 | 前端维护 | 否 |
| API Key 明文 localStorage | 🟢 低 | 安全性 | 否（设计权衡） |

---

## 5. 相对旧版报告的关键演进总结

| 维度 | 旧版（2026-08-12） | 当前（2026-09-03） | 变化 |
|------|-------------------|-------------------|------|
| **后端结构** | `main.py` 单文件 1138 行 | 10 模块多包，`main.py` 57 行 | 🟢 彻底重构 |
| **会话管理** | 无 TTL，内存泄漏 | 2 小时 TTL 驱逐 | 🟢 已解决 |
| **前端状态** | 组件直接改 reactive | readonly + Actions 单向流 | 🟢 已重构 |
| **世界硬编码** | 4 套专属 summary 函数 | 零硬编码 spec 驱动 | 🟢 已消除 |
| **内置世界数** | 4 个 | 10 个 | 🆕 +6 |
| **HTTP 端点** | 18 个 | 19 个 | 🆕 +1（export-all/import-all 等） |
| **测试文件** | 17 个 | 23 个 | 🆕 +6 |
| **测试用例** | 143（README 声明） | 214（实测 213 过 + 1 失败） | ⚠️ 1 个失修 |
| **token 预算** | tier + 1200 | tier + 6000 | 🆕 调整（引发测试失修） |
| **反水铁律** | 无 | 全局 `_ANTI_LOOP_RULES` | 🆕 新增 |
| **主题系统** | 硬编码 amber-600 | primary 主题变量 + 自定义背景图 | 🆕 新增 |
| **设置面板** | 分散（FreedomControl + 首页底部） | SettingsPanel 四分组统一收纳 | 🆕 重构 |
| **日志** | 无日志框架 | logging 模块接入 | 🟢 改善 |

**总体评价**：项目在约三周内完成了**一次系统性的架构重构**——God Object 拆解、会话 TTL、前端单向数据流、世界硬编码消除四项旧版最高优先级隐患全部落地，并新增 6 个内置世界与多项健壮性改进（反水铁律、token 预算、越权防线、选项扩展）。**唯一可观测的新回归是测试套件失修**（token 预算调整未同步 `test_freedom` 断言），应作为下一迭代的首要修复项。

---

> **报告结束。**
>
> 本报告基于对 **17 个后端核心模块 + 10 个前端核心文件** 的扫描生成，辅以 15 个子代理并行模块剖析。行号引用基于 2026-09-03 的代码快照（HEAD `e02077b`）。
